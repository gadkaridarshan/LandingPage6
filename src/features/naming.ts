## Scripts

| Script            | What it does                                            |
| ----------------- | ------------------------------------------------------- |
| `npm start`       | Serves `./public` on `http://localhost:3000`            |
| `npm run serve`   | Same as `npm start`                                     |
| `npm run fix:eperm` | Runs the macOS EPERM remediation script               |

## Quality bar

LandingPage6 is held to a deliberate standard:

- **Typography** — Inter from Google Fonts, with a clear scale and accessible sizes.
- **Background** — subtle gradients and patterns across sections; never a flat, unfinished page.
- **Best practices** — semantic HTML, BEM-style class naming, no inline styles,
  accessible forms (labels, `aria-*`, keyboard navigation), and maintainable code.